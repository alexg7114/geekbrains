{% extends 'layout.twig' %}

{% block title %}MyApp / Catalog / Categories{% endblock %}
{% block content %}
    <h2>Каталог</h2>
    {% for cat in categories %}
        <li><a href="/catalog/{{ cat.id }}">{{ cat.title }}</a></li>
    {% endfor %}
{% endblock %}